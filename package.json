{
  "name": "shikaku",
  "version": "1.0.0",
  "description": "Shikaku - Koa2 + Vue3 + MySQL Web Application",
  "scripts": {
    "install-all": "cd server && yarn install && cd ../client && yarn install",
    "dev": "concurrently \"cd server && yarn dev\" \"cd client && yarn dev\"",
    "server": "cd server && yarn dev",
    "client": "cd client && yarn dev",
    "build": "cd client && yarn build"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}

